import React from'react';import{AlertTriangle,CheckCircle2,Film,Play,Search,X}from'lucide-react';import type{Video}from'./types';
export function Button({children,kind='default',...p}:React.ButtonHTMLAttributes<HTMLButtonElement>&{kind?:string}){return <button className={`btn ${kind}`} {...p}>{children}</button>}
export function Status({value}:{value:string}){const v=value.replaceAll('_',' ');return <span className={`status ${value}`}>{value==='completed'?<CheckCircle2/>:value.includes('drm')||value==='failed'?<AlertTriangle/>:null}{v}</span>}
export function Progress({value}:{value:number}){return <div className="progress"><span style={{width:`${Math.max(0,Math.min(100,value))}%`}}/></div>}
export function Modal({title,onClose,children}:{title:string;onClose:()=>void;children:React.ReactNode}){return <div className="modal-backdrop" onMouseDown={onClose}><section className="modal" onMouseDown={e=>e.stopPropagation()}><header><h2>{title}</h2><button className="icon-btn" onClick={onClose}><X/></button></header>{children}</section></div>}
export function SearchBox({value,onChange,placeholder='Search'}:{value:string;onChange:(v:string)=>void;placeholder?:string}){return <label className="search"><Search/><input value={value} onChange={e=>onChange(e.target.value)} placeholder={placeholder}/></label>}
export function MediaThumb({video,onPreview}:{video:Video;onPreview?:()=>void}){return <button className="thumb" onClick={onPreview} aria-label={`Preview ${video.title}`}>{video.thumbnail_url?<img src={video.thumbnail_url} alt=""/>:<Film/>}<span><Play/> Preview</span></button>}
